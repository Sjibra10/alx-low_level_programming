#include "main.h"
/**
* add - adds two integers and returns the result
* @a: int to be added to t
* @t: int to be added to a
* Return: sum of a and b
*/

int add(int a, int t)
{
	int b;

	b = a + t;

return (b);
}

